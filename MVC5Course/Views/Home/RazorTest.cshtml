@model int[]
@*練習用Razor 輸出*@
@{}
@for(int i =0; i<Model.Length; i++)
{
    @Model[i]
}
@Environment.NewLine

@foreach (var i in @Model)
{
    @i
}

@foreach(var i in @Model)
{
    @(i +" ") 
}

<ul>
    @foreach (var i in @Model)
    {
        @:  <li>@i</li>
    }
</ul>